[model]
name = Redirect

[fields.target]
label = Redirect Target
type = string
description = Target is of type 'string' to allow relative paths. Converted to url in the template.

[fields.absolute]
type = boolean
description = Don't resolve the url, use it as it is
